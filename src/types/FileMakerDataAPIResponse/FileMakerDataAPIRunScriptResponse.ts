type FileMakerDataAPIRunScriptResponse = {
  /**
   * The response error code from the script (0 is no error).
   */
  scriptError: string

  /**
   * The result from the script.
   */
  scriptResult: string
}

export default FileMakerDataAPIRunScriptResponse
